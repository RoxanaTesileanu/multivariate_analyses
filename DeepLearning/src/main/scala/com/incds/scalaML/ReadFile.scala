package com.incds.scalaML
 
object ReadFile {

/*def readFileClassif (filename: String, delim: String) : Vector((Double*)), Vector (Char) = {

val src = scala.io.Source.fromFile(filename)
val data = src.getLines.map(_.split(delim)).toArray.drop(1)
src.close()
val len = data(0).length
val data2 = data.map(x => List(x(0), x(1), x(2), x(3)))
val data3 = data2.map{ case(List(param1, param2, param3, param4)) => List(param1.toDouble, param2.toDouble, param3.toDouble, param4.toDouble) }

val dataMatrix= data3.map { case(List(param1, param2, param3, param4)) => List(param1, param2, param3)}

val dataLabels = 

}
*/





}

/* References:

Peter Harrington - "Machine Learning in Action", 2012
Jason Swartz - "Learning Scala", 2015

*/
